

# this makefile does nothing yet, as there is nothing to make
# but eventually, as features are added, each source file will generally
# compile to it's own statically linked .a file that can be included
# individually based on needs

# also, try to keep this makefile simple. Take inspiration from the makefile for LPeg
